package SelQuiz03ScreenshotActions;

public class Q04 {
}
/*
how do we scroll in selenium?
Group of answer choices

((JavascriptExecutor) driver).executeScript("arguments[0].scrollIntoView(true);", element.location);

No answer text provided.

((JavascriptExecutor) driver).executeScript("arguments[0].scrollIntoView(true);", element.point);

((JavascriptExecutor) driver).executeScript("arguments[0].scrollIntoView(true);", element);
 */