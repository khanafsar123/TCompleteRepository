package SelQuiz03ScreenshotActions;

public class Q01 {
}
/*
What is the recommended method to capture a screenshot using Webdriver?
Group of answer choices

File screenshot = ((TakesScreenshot)driver).getScreenshotAs(OutputType.file); FileUtil.copyFile(screenshot, new File("c:\screenshot.jpeg"));

No answer text provided.

File screenshot = ((TakesScreenshot)driver).getScreenshotAs(OutputType.FILE); FileUtils.copyFile(screenshot, new File());

File screenshot = ((TakesScreenshot)driver).getScreenshotAs(OutputType.FILE); FileUtils.copyFile(screenshot, new File("c:\screenshot.png"));
 */