package SelQuiz01;

public class Q12 {
    /*
In webdriver, what is the method that counts the number of elements?
Group of answer choices

driver.findElement(By.name("search")).size()

driver.findElements(By.id("search")).length()

driver.findElements(By.name("search")).size().....

driver.selectAllOptions()
     */
}
